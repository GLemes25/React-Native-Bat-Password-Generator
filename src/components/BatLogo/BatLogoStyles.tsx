import { StyleSheet } from 'react-native';

export const BatLogoStyles = StyleSheet.create({
  title: {
    fontSize: 30,
    fontWeight: 'bold',
    textAlign: 'center',
    color: '#e5bf3c',
  },
});
